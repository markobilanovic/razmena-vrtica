{
  "name": "razmena-vrtica-root",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev:backend": "npm run start:dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "db:dev": "docker compose -f docker-compose.dev.yml up -d",
    "db:dev:stop": "docker compose -f docker-compose.dev.yml down",
    "build": "npm run build --workspaces",
    "build:shared": "npm run build -w shared",
    "build:backend": "npm run build -w backend",
    "build:frontend": "npm run build:shared && npm run build -w frontend",
    "build:vercel": "npm run build -w backend",
    "test": "npm run test --workspaces",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "migrate:prod": "cd backend && npm run migration:run",
    "seed:prod": "cd backend && npm run seed:prod",
    "test:supabase": "cd backend && npm run test:supabase"
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  },
  "dependencies": {
    "passport-google-oauth20": "^2.0.0"
  }
}
